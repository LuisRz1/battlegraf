import type { Metadata } from "next";
export const metadata: Metadata = { title: "Panel institucional | BattleGraf", description: "Prototipo navegable del panel escolar de BattleGraf." };
export default function AdminLayout({children}:{children:React.ReactNode}){return children}
