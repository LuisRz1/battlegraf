import type { Metadata } from "next";
export const metadata: Metadata = { title: "Panel institucional | BattleGraph", description: "Prototipo navegable del panel escolar de BattleGraph." };
export default function AdminLayout({children}:{children:React.ReactNode}){return children}
